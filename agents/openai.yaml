interface:
  display_name: "X Mutual Pilot"
  short_description: "Review and execute X interactions with safety gates"
  default_prompt: "Use $x-mutual-pilot to sync X relationships, review candidates, and keep every write behind the required safety gates."
